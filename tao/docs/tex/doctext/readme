
    You should set the variable

setenv DOCTEXT_PATH ${TAO_DIR}/docs/tex/doctext

(only for TAO developers, not included in the public
TAO distribution)

contains the "formating" files used by doctext to format
the HTML manual pages.

