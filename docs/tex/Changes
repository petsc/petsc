
  This is list of recent PETSc changes before they are 
transported into the docs/changes.html file. (Because it
is a pain to always check that file out of SourceSafe to 
add to it).

  Added VecSetBlockSize(), VecSetValuesBlocked(), VecSetValuesBlockedLocal()
        VecSetLocalToGlobalMappingBlocked()

  Changed docs/www to docs/manualpages and docs/www/www.html docs/manualpages/manualpages.html

  Added VecCreateShared()
