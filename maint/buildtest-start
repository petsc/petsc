#! /bin/csh

set SSH=ssh
set SCP=scp
set dir=`dirname $0`
set ARCH=$1
set DASHN=-n
source $dir/buildtests/$1


echo "start build: $MACH $ARCH `date`"
$SSH $MACH $DASHN "$TMP/maint/testdata $ARCH"
echo "end build: $MACH $ARCH  `date`"

