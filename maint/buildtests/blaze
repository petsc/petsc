#! /bin/csh

set TMP=/sandbox/petsc/petsc-dev
set MACH=blaze
set ARCH=solaris_gnu
#
set day=`date | cut -f1 -d" "`

if ($day == Sun || $day == Wed $day == Sat) then
  set ARCH=solaris_uni
else if ($day == Thu || $day == Fri) then
  set ARCH=solaris64
endif




