#requirespackage  'PETSC_HAVE_AMS'

ALL: lib

CFLAGS   =
FFLAGS   =
SOURCEC  = kspams.c
SOURCEF  =
SOURCEH  =
LIBBASE  = libpetscksp
DIRS     =
LOCDIR   = src/ksp/ksp/interface/ams/
MANSEC   = KSP

include ${PETSC_DIR}/conf/variables
include ${PETSC_DIR}/conf/rules
include ${PETSC_DIR}/conf/test
