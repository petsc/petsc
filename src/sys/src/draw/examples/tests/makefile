ITOOLSDIR = ../..
ALL: 

CFLAGS   = $(OPT) $(INCLUDEDIRS)$(CONF) $(COPT)
SOURCEC  = 
SOURCEF  =
WSOURCEC = 
SOURCEH  =
OBJSC    =
WOBJS    = 
OBJSF    =
LIBBASE  = libpetscdraw
EXAMPLES = 
EXAMPLESC= 
SYSLIB   = $(LDIR)/libpetscdraw.a $(LDIR)/libpetscsys.a \
           $(LAPACK) $(BLAS) \
           $(CLIB) $(SLIB) $(FCLIB) -lm 
LINCLUDE = $(SOURCEH)

include $(ITOOLSDIR)/bmake/$(PARCH)/$(PARCH)
