
ThingsToDo:
  - make this run
  - fix multimodel flags
  - set up scatter in bc.c for trailing edge BC
  - incorporate new FD with coloring via DAGetColoring (jacob.c)

New routines for full potential code:
  xnd_fp.F
    IC_FP() - initial conditions
  resid_fp.F
    localfortfct_fp() - residual wrapper
    resid_fp() - interior nodes
    residbc_fp() - boundary nodes (ibvwork.h used for Euler)
  rbuild_fp.F
    rbuild_direct_fp() - assembly of interior vector values (local to global)
  
