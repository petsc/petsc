PETSC_DIR = ../..

CFLAGS   = -I$(PETSC_DIR)/include -I$(PETSC_DIR) $(CONF) $(PCONF)
SOURCEC  =
SOURCEF  =
SOURCEH  = 
OBJSC    =
OBJSF    =
LIBBASE  = libpetscvec
DIRS     = vgrid sda

include $(PETSC_DIR)/bmake/$(PETSC_ARCH)/$(PETSC_ARCH)

