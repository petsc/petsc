%
% This file contains definitions for the page types.  The types are:
%
% chapter
% part
% regular
%
% These are set by using the \pagestyle or \thispagestyle command
%
% headstrutbox makes the space between the rules in the header constant;
% the depth is picked to put the running header in the center (vertically)
% of the header
\newbox\headstrutbox
\setbox\headstrutbox=\hbox{\vrule height.20in depth.10in width\z@}
\def\headstrut{\relax\ifmmode\copy\headstrutbox\else\unhcopy\headstrutbox\fi}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% pagerule is a rule the full width of the page
\def\pagerule#1{\hbox to #1{\vbox to .25pt{\hbox to #1{\leaders\hrule height .25pt \hfill}\vss}}}
%
% pageruleb is a BOLD rule the full width of the page
\def\pageruleb{\hbox to \hsize{\leaders\hrule height 1.5pt \hfill}}

\def\section{\secdef\CMDA\CMDB}
\def\basesection#1{\sectionmark{#1}
\vskip 3ex \hbox to \hsize{\Large\bf #1\hfil}\vskip -1.3ex plus .2ex}
\def\CMDA[#1]#2{\basesection{#2}}
\def\CMDB#1{\basesection{#1}}

\topmargin -.23in
\textwidth 6.3in
\def\pagewidth{6.3in}
\def\marginsize{0.0in}
\def\thanks#1{\relax}
\parindent0pt
\textheight=9.8in
\hoffset=-.7in
\voffset=-.8in
\parskip=10pt

\let\titlelogo=\anllogo
\def\oldraggedright{\rightskip\z@ plus8em \spaceskip.3333em \xspaceskip.5em\relax}

\def\maketitle{\hbox to \hsize{%
		\setbox0=\vbox{\hsize = 4.5in 
	                       \oldraggedright %
			       \noindent \Huge \@title\par\vskip-\parskip}
		\ifdim\ht0>1in 
		\setbox0=\vbox{\hsize = 4.5in 
	                       \oldraggedright %
			       \noindent \huge \@title\par\vskip-\parskip}
		\fi 
	        \ifdim\ht0>1in
		\vbox{
                  \hbox to 4.5in{\vbox{\vfill\box0}}}%
                \else
		\vbox{
                  \hbox to 4.5in{\vbox to 1in{\vfill\box0}}}%
	        \fi
	        \hfill \vspace{.5cm}
	        \vbox{
	          \hbox to 1in{\vbox to 0in{\vskip0.0in\hbox{\hskip-.4in %
	    			\titlelogo}\vss}\hss}}}
\vskip2pt\vbox to 3pt{\pagerule{5.025in}\vskip-12.85pt\nobreak\vskip 2pt plus .5pt \nobreak\pagerule{5.075in}}
 \let\thanks\relax
\gdef\@thanks{}\gdef\@author{}\gdef\@title{}\let\maketitle\relax}

\def\anllogo{\vbox to 0pt{\vskip-1.2in \epsfxsize=1.5in \epsfysize=1.5in 
\hskip .01in \epsfbox{argonne.ps}\vss}}

\let\titlelogo=\anllogo

%
% Remove hypertext citations from paper copy
\def\hcite#1{}
%
% This is the end-of-page (further info) 
\def\email#1{\gdef\theemail{#1}}
\def\phone#1{\gdef\thephone{#1}}
\def\contact#1{\gdef\theauthor{#1}}
\def\division#1{\gdef\thedivision{#1}}
\def\thedivision{Mathematics and Computer Science Division}

\def\makeinfo{\section*{Development Team}
\vspace{-.2cm}
{\begin{center}
\theauthor\\
\thedivision \\
Argonne National Laboratory \\
\theemail\\
\end{center}
}}

\def\code#1{{\tt #1}}

