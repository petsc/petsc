interface:
  display_name: "PETSc CodeGraph"
  short_description: "Navigate PETSc code, dispatch, and object lifecycle"
  default_prompt: "Use $codegraph to find existing PETSc functionality, trace runtime dispatch and object lifecycle, and assess a proposed change's blast radius."
