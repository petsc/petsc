# -*- makefile -*-

ALL: poisson3d.exe

CPPFLAGS= 
CFLAGS=
FFLAGS=
LOCDIR= 

poisson3d.exe: poisson3d.o
	${FC} ${FFLAGS} -c -o del2lib.o del2lib.f90
	${CLINKER} -o $@ $< del2lib.o ${PETSC_KSP_LIB}
	${RM} poisson3d.o del2lib.o

include ${PETSC_DIR}/conf/variables
include ${PETSC_DIR}/conf/rules
