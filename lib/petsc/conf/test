# -*- mode: makefile -*-
#

ifeq ($(RM),true)
.SECONDARY: $(%.o) $(%f.o) $(%f90.o)
endif


%f90: %f90.o
	-${FLINKER} -o $@ $^  ${PETSC_LIB}
%f: %f.o
	-${FLINKER} -o $@ $^  ${PETSC_LIB}
% : %.o
	-${CLINKER} -o $@ $^  ${PETSC_LIB}
	${DSYMUTIL} $@


include ${PETSC_DIR}/lib/petsc/conf/test.common
