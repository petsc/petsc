PETSC_DIR = ../..

CFLAGS   = $(OPT) $(PETSC_INCLUDE) -I.. -I$(IPETSCDIR) $(CONF) $(PCONF)
SOURCEC  =
SOURCEF  =
SOURCEH  = petsc.h vec.h snes.h da.h draw.h mat.h ksp.h pc.h sles.h viewer.h is.h
OBJSC    =
OBJSF    =
LIBBASE  = libpetscvec
DIRS     = 
MANSEC   = 7
LOCDIR   = include/

runexamples:

include $(PETSC_DIR)/bmake/$(PETSC_ARCH)/$(PETSC_ARCH)


