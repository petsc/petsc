IPETSCDIR = ../

CFLAGS   = $(OPT) $(PETSC_INCLUDE) -I.. -I$(IPETSCDIR) $(CONF)
SOURCEC  =
SOURCEF  =
SOURCEH  = petsc.h vec.h
OBJSC    =
OBJSF    =
LIBBASE  = libpetscvec
DIRS     = 
MANSEC   = 7

runexamples:

include $(IPETSCDIR)/bmake/$(PETSC_ARCH)/$(PETSC_ARCH)


